const INITIAL_STATE = {
  questions: [],
};

const question = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  case 'value':

    break;

  default:
    return state;
  }
};

export default question;
